spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/security_demo
  jpa:
    generate-ddl: true
    properties:
      hibernate:
        ddl-auto: update
        dialect: org.hibernate.dialect.MySQL5Dialect